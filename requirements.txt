# Multilingual Voice Assistant (LiveKit Agents)
# Python 3.10+ recommended. Install: pip install -r requirements.txt

# --- LiveKit core and agents framework ---
livekit
livekit-api
livekit-agents
livekit-plugins-groq
livekit-plugins-silero
livekit-plugins-cartesia

# --- Web Server (Token Server) ---
fastapi
uvicorn

# --- Config & HTTP ---
python-dotenv
requests
